export interface SingleRangeSliderProps {
  min: number;
  max: number;
  initialValue: number;
  step: number;
}
export interface DoubleRangeSliderProps {
  min: number;
  max: number;
  initialMin: number;
  initialMax: number;
  step: number;
}
